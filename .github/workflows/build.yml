name: Gerar Executavel e Release Windows

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: windows-latest
    permissions:
      contents: write 

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Instalar Dependencias
      run: |
        python -m pip install --upgrade pip
        pip install fastapi uvicorn pydantic pystray pillow pywin32 requests pyinstaller

    - name: Injetar a Versão no Código Python
      run: |
        python -c "codigo = open('config.py', encoding='utf-8').read(); open('config.py', 'w', encoding='utf-8').write(codigo.replace('DEV_VERSION', '${{ github.ref_name }}'))"

    - name: Gerar o EXE
      run: |
        pyinstaller --onefile --noconsole --name "Agente_Impressao" main.py

    - name: Publicar Release Oficial
      uses: softprops/action-gh-release@v2
      with:
        files: dist/Agente_Impressao.exe
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}